import "element-remove";
import "core-js/features/array/fill";
import "core-js/features/array/fill";
import "core-js/features/array/includes";
import "core-js/features";
import "core-js/features/promise";
import { LaunchProps } from "./types";
export interface CustomWindow extends Window {
    PayHere: PayHereClient;
}
declare class PayHereClient {
    private modalOpen;
    private onSuccess;
    private onFailure;
    private onClose;
    constructor();
    launch({ embedURL, customerName, customerEmail, disableCustomer, amountInCents, hideAmount, customFields, onSuccess, onFailure, onClose, }: LaunchProps): void;
    private setupButtonListeners;
    private setupIframeListeners;
}
declare const PayHere: PayHereClient;
export default PayHere;
