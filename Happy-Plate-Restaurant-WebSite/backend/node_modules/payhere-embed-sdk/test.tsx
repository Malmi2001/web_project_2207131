import PayHere from "./dist/react"

const App = () => {
  return (
    <div>
      <PayHere
        open={false}
        selector=".payhere-modal"
        embedURL=""
        amountInCents={4000}
        onSuccess={() => {}}
        onFailure={() => {}}
        onClose={() => {}}
      />
    </div>
  )
}

export default App
